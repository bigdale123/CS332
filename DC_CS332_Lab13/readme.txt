# Compiling and Running
- To compile, run the follfowing command:

    `gcc lab12.c -o lab12 -lpthread`

- To run, use the following syntax:

    `./lab12 <# of elements> <# of threads>`

    - Note that the # of threads cannot exceed the # of elements.